#!/usr/bin/env bash
git config --global user.name "Aristid Breitkreuz"
git config --global user.email "aristidb@gmail.com"
git config --global push.default "simple"
git config --global core.excludesfile ~/dotfiles/gitignore-global
